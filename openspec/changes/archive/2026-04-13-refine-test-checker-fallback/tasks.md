## 1. Implementation

- [x] 1.1 Update "No. of Items" `onChange` handler in `TestCheckerPage.tsx` to fallback to 5 when the input is empty or 0.

## 2. Verification

- [x] 2.1 Verify grid shows 5 items when the input is cleared.
- [x] 2.2 Verify grid shows 5 items when the input is "0".
- [x] 2.3 Verify grid shows 10 items when the input is "10".
